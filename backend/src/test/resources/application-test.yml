spring:
  datasource:
    # Testcontainers will override these properties dynamically
    url: jdbc:postgresql://localhost:5432/invoiceme_test
    driver-class-name: org.postgresql.Driver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
  security:
    user:
      name: testuser
      password: testpass

